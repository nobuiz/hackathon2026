fastapi==0.138.0           # bumped to satisfy arize-phoenix 17.9.0 (_IncludedRouter)
uvicorn[standard]==0.34.0
python-dotenv==1.2.2
# Sponsor integrations — all optional at runtime; the app degrades gracefully without keys.
anthropic==0.111.0         # Claude — extraction + denial-risk reasoning
redis==5.2.1               # inter-agent session state
sentry-sdk==2.20.0         # exception capture (Best Use of Sentry API)
deepgram-sdk==3.7.7        # voice intake for phone referrals
requests==2.32.3           # Browserbase session API
# Optional extras — uncomment to run the live versions of these engines:
# playwright==1.49.0                              # drive the Browserbase hosted browser
arize-phoenix==17.9.0                            # Phoenix UI + OTLP collector (localhost:6006)
arize-phoenix-otel==0.16.1                       # Phoenix tracing helpers
opentelemetry-sdk==1.42.1                        # tracer provider / span processors
opentelemetry-exporter-otlp-proto-http==1.42.1  # OTLP span export to Phoenix
opentelemetry-semantic-conventions==0.63b1       # provides GenAiOutputTypeValues (phoenix 17.9)
# conductor-python==1.1.6                         # run the Orkes Conductor workers
